services:
  newapi-watchdog:
    image: ghcr.io/charyeahowo/newapi_watchdog:latest
    container_name: newapi-watchdog
    restart: always
    environment:
      - TZ=Asia/Shanghai
    ports:
      - "8088:8088"
    volumes:
      - watchdog-data:/data

volumes:
  watchdog-data:
