services:
  newapi-watchdog:
    build: .
    container_name: newapi-watchdog
    restart: unless-stopped
    ports:
      - "8088:8088"
    volumes:
      - watchdog-data:/data
    networks:
      - newapi

networks:
  newapi:
    external: true

volumes:
  watchdog-data:
